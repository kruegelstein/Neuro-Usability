import ActionTypes from '../constants';

let initial = {
  series: []
}

function series(state = { ...initial }, action) {
  switch(action.type) {

    default:
      return state
  }
}

export default series;
